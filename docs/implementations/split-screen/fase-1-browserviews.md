# Split Screen Phase 1 — Historical Note

This phase document does not match the current codebase.

## Current state

- no split-screen API routes are registered
- no `GET /split/status` endpoint exists
- no split manager is wired into `src/api/server.ts`

Treat this file as historical planning material only.

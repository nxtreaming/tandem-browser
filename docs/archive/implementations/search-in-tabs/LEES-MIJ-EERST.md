# Search in Tabs — Historical Note

This implementation pack assumed a `GET /tabs/closed` API that does not exist
in the current code.

## Current state

- `GET /tabs/list` exists
- `POST /tabs/focus` exists
- `POST /tabs/open` exists
- `GET /tabs/closed` does not exist

Treat the older notes in this folder as a design direction, not as current API
documentation.
